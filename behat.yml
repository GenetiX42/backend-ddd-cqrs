default:
  suites:
    default:
      paths:
        - features
      contexts:
        - FeatureContext
  autoload:
    '': features/bootstrap

persistence:
  suites:
    default:
      paths:
        - features
      contexts:
        - DatabaseFeatureContext
  autoload:
    '': features/bootstrap
